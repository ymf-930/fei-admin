// dark皮肤
[data-theme=dark] {
  // logo样式
  .layout-aside .logo {
    border-bottom: none;
    h1 {
      color: #ffffff;
    }
  }
  // menu样式
  .fei-menu {
    .fei-menu-item {
      color: $color-text-white-light;
      background: getMenuBg();
      &.is-disabled {
        background: getMenuBg() !important;
        opacity: .9;
        &:hover {
          color: $color-text-white-light;
        }
      }
      &:hover {
        color: $color-text-white;
      }
      &.is-active {
        color: $color-text-white;
        background: getColor();
      }
    }
    .fei-submenu__title {
      color: $color-text-white-light;
      background: getMenuBg();
      &:hover {
        color: $color-text-white;
      }
    }
    .fei-menu-item-group__title {
      color: $color-text-white-disable;
      background: getMenuBgActive();
    }
    .fei-submenu .fei-menu--inline {
      .fei-menu-item, .fei-submenu__title {
        background: getMenuBgActive();
      }
      .fei-menu-item {
        &.is-active {
          color: $color-text-white;
          background: getColor();
        }
      }
    }
    // 收起菜单的样式
    &.fei-menu--collapse {
      .fei-menu-item {
        &.is-active {
          color: getColor();
          background: getMenuBgActive();
          &::after {
            position: absolute;
            top: 0;
            left: 0;
            width: 2px;
            height: 100%;
            background-color: getColor();
            content: '';
            z-index: 1;
          }
        }
      }
      .fei-submenu.is-active {
        .fei-submenu__title {
          color: getColor();
          background: getMenuBgActive();
        }
        &::before {
          position: absolute;
          top: 0;
          left: 0;
          width: 2px;
          height: 100%;
          background-color: getColor();
          content: '';
          z-index: 1;
        }
      }
    }
    &.fei-menu--horizontal {
      .fei-menu-item {
        color: $color-text-white-light;
        background: getMenuBg();
        &:hover {
          color: $color-text-white;
        }
        &.is-active {
          color: $color-text-white;
          background: getColor();
          border-bottom-color: getColor();
        }
      }
      .fei-submenu {
        .fei-submenu__title {
          &:not(.is-disabled):hover, &:not(.is-disabled):focus {
            color: $color-text-white;
            background: getMenuBg() !important;
          }
          &:hover {
            color: $color-text-white;
            background: getMenuBg() !important;
          }
        }
        &.is-active {
          .fei-submenu__title {
            border-bottom-color: getColor();
            color: getColor();
          }
        }
      }
    }
  }
  // menu-popup
  .fei-menu--popup {
    background: getMenuBg();
    .fei-menu-item, .fei-submenu__title {
      color: $color-text-white-light;
      background: getMenuBg();
      &:hover {
        color: $color-text-white;
      }
      &.is-active {
        color: $color-text-white;
        background: getColor();
      }
    }
  }
}

