// 所有颜色取值应统一写在此函数中，否则会存在很多定义变量的位置，不利于维护
getActive() {
  var(--fei-color-primary-active)
}
getColor() {
  var(--fei-color-primary)
}
getHover() {
  var(--fei-color-primary-light1)
}
getLighten3() {
  var(--fei-color-primary-light3)
}

getLighten5() {
  var(--fei-color-primary-light5)
}

getLightenHover() {
  var(--fei-color-primary-light6)
}

getShadow() {
  var(--fei-color-input-shadow)
}
getLightenHover() {
  var(--fei-color-primary-light6)
}

getMenuBg() {
  var(--menu-bg-color)
}
getMenuBgActive() {
  var(--menu-bg-active-color)
}
